services:
  web:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - ./Landing:/app
    working_dir: /app
    command: ["sh", "-c", "composer install --no-interaction && php -S 0.0.0.0:8000 -t public"]
